# このリポジトリで作業するときの取り決め

## 本番への反映

**確認を取らずに `main` へ push してよい。** 毎回「反映していいですか」と聞かない。

`main` への push は Vercel の自動デプロイに繋がる＝そのまま本番へ出る、という前提で扱う。
作業ブランチに置いたまま報告して終わりにしない。直したものは反映まで持っていく。

### 反映する前に必ず通すもの

1つでも落ちていたら push しない。直してから出す。

```bash
npx tsc -b          # 型
npm run lint        # oxlint
npm test            # ユニット（api/ 配下のサーバー関数も含む）
npm run build       # 本番ビルド
npm run build:artifact
```

`dist-artifact/` はビルド成果物だがコミットする運用なので、`src/` を触ったら
`npm run build:artifact` をやり直してから一緒にコミットする。

E2E（`npm run test:e2e`）は、画面の導線を変えたときに走らせる。

### 反映しても、それだけでは終わらないこと

デプロイすると `api/checkAppUpdate.ts` が版の変化を見つけ、購読中の全端末へ
「アプリがアップデートされました」のプッシュ通知が飛ぶ。細かい修正を何度も出すと
そのたびに通知が鳴るので、まとめられる直しはまとめて出す。

### それでも先に確認すること

自動で進めてよいのは「書いたコードを本番に出す」ところまで。以下は別で、必ず聞く。

- 履歴の書き換え（force push、push 済みコミットの作り直し）
- `main` 以外への push、ブランチの削除
- Supabase の本番データやスキーマを消す・作り替える操作
- APIキーや環境変数の入れ替え・削除
- Pull Request の作成（頼まれたときだけ作る）

## 言葉づかい

コミットメッセージ・コード内のコメント・画面の文言はすべて日本語。
コメントは「何をしているか」ではなく「なぜそうしたか」、とくに
**そうしないと何が壊れるか** を書く（既存のコメントがその書き方で揃っている）。
